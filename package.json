{
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist",
    "deploy-docs": "npm run build && git add docs && git commit -m 'Deploy update' && git push"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.1",
    "bootstrap": "^5.3.5",
    "chart.js": "^4.4.9",
    "jquery": "^3.7.1"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^6.2.1"
  }
}